export enum EventChannel {
  None = 'none',
  Product = 'product',
  Basket = 'basket',
  Auth = 'auth',
}

export enum EventTopic {
  None = 'none',
  Loading = 'loading',
  BasketContentAmended = 'basket-content-amended',
  SignOutAttempt = 'sign-out-attempt',
}
