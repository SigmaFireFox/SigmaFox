import { MultiCaseSwitchCaseDirective } from './multi-case-switch-case.directive';

describe('MultiCaseSwitchCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiCaseSwitchCaseDirective();
    expect(directive).toBeTruthy();
  });
});
